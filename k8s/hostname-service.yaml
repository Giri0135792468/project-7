apiVersion: v1
kind: Service

metadata:
  name: hostname-service
  namespace: project-7

spec:
  type: ClusterIP

  selector:
    app: hostname-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80