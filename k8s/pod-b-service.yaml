apiVersion: v1
kind: Service

metadata:
  name: pod-b-service
  namespace: project-7

spec:
  type: ClusterIP

  selector:
    app: pod-b

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80